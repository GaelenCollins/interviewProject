/** Welcome copy only — no hardcoded check findings. */
export const AI_RESPONSES = {
  welcome:
    'Hi. Drop a Distributor Quote (Excel Source) and a Dynamix Customer Quote (PDF) and I\'ll start the check right away.',
}
